# Copy this to .env and fill in values at the hackathon
# DO NOT commit .env to git

# OpenAI — you'll get $100 credits at check-in (use personal workspace!)
OPENAI_API_KEY=sk-...

# Anthropic — for Claude Code (usually set by `claude login`)
ANTHROPIC_API_KEY=sk-ant-...

# Claude-Mem worker port (auto-detected, but override here if needed)
# CLAUDE_MEM_WORKER_PORT=3456
